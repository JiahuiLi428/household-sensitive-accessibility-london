# Greater London OSM source

Expected local file: `greater-london-latest.osm.pbf`.

Download source:
`https://download.geofabrik.de/europe/united-kingdom/england/greater-london-latest.osm.pbf`

The PBF is retained locally but excluded from the normal Git commit because it
exceeds the standard single-file limit. Its SHA-256 checksum is recorded in
`data/CHECKSUMS.sha256`.
